const { generateRoster } = require('../roster');

describe('Board', () => {
  // Тестов нет :\
  // Попробуешь написать?

  beforeEach(() => {
    // board = .....
  });

  describe('place header', () => {
    // Код тут.
  });

  describe('init board', () => {
    // Код тут.
  });
});
